export enum ERoutesPath {
  HOME = "/",
  TODO = "/todo",
  WEATHER = "/weather",
  CALENDAR = "/calendar",
  SETTINGS = "/settings",
  AUTH = "/auth",
}

export enum ERoutesTitle {
  HOME = "home",
  TODO = "todo",
  WEATHER = "weather",
  CALENDAR = "calendar",
  SETTINGS = "settings",
  AUTH = "auth",
}
